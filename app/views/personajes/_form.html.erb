<%= form_with(model: personaje, local: true) do |form| %>
  <% if personaje.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personaje.errors.count, "error") %> prohibited this personaje from being saved:</h2>

      <ul>
      <% personaje.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :personaje_name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age, id: :personaje_age %>
  </div>

  <div class="field">
    <%= form.label :creator %>
    <%= form.text_area :creator, id: :personaje_creator %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
